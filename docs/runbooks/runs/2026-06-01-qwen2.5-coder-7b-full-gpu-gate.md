# Run Record: `qwen2.5-coder:7b` Full-GPU Gate

## Identity

- Date: `2026-06-01`
- Stand: `win11-local-rx6800`
- Endpoint: `http://localhost:11434`
- Model: `qwen2.5-coder:7b`
- Ollama artifact size: `4.7 GB`
- Artifacts: `.artifacts/claude-ollama-headless/20260601T210048/`
- Runner option: `-RequireFullGpu`

## Result

| Stage | Seconds | Prompt marker observed | Real tool loop | Placement | Verdict |
|---|---:|---|---|---|---|
| `handshake` | `36.586` | Yes | n/a | `100% GPU` | Pass |
| `tool` | `33.544` | Yes | No | `100% GPU` | Fail: printed tool-call JSON |
| `agent` | `34.459` | No | No | `100% GPU` | Fail: printed `Read` JSON and made no edit |

## Placement Evidence

```text
qwen2.5-coder:7b
8.2 GB
100% GPU
CONTEXT 32768
```

Ollama server log:

```text
offloaded 29/29 layers to GPU
ROCm0 model buffer size = 4168.09 MiB
ROCm0 KV buffer size = 1792.00 MiB
```

The server log also contains small host-side runtime buffers. These are not
model-weight spill: Ollama reports `100% GPU`, and all `29/29` model layers
were offloaded to the GPU.

## Tool-Loop Evidence

The model printed a Bash request as plain text:

```json
{
  "name": "Bash",
  "arguments": {
    "command": "python -c \"print('HEADLESS_TOOL_OK')\""
  }
}
```

It did not emit a Claude Code structured `tool_use` block.

The agent stage failed similarly by printing a `Read` request as JSON text.
No target file was created.

## Decision

Exclude `qwen2.5-coder:7b` from the coder fixture suite on
`win11-local-rx6800`.

It passes the full-GPU placement gate but fails real Claude Code tool
execution. The failure reproduces the historical Linux result for the related
`qwen2.5-coder:7b-instruct-q4_K_M` tag.
