# Run Record: `granite4.1:8b-ctx32k` Full-GPU Gate

## Identity

- Date: `2026-06-01`
- Stand: `win11-local-rx6800`
- Endpoint: `http://localhost:11434`
- Upstream model: `granite4.1:8b`
- Local profile: `granite4.1:8b-ctx32k`
- Model details: `8.8B`, `Q4_K_M`
- Capabilities: completion, tools
- Profile Modelfile: `tools/modelfiles/granite4.1-8b-ctx32k.Modelfile`
- Default-profile artifacts: `.artifacts/claude-ollama-headless/20260601T211520/`
- Tuned-profile artifacts: `.artifacts/claude-ollama-headless/20260601T212828/`
- Runner option: `-RequireFullGpu`

## Why A Local Profile Is Required

The upstream `granite4.1:8b` tag advertises a `131072` token context window.
With that default, Ollama allocated a `20480 MiB` CPU KV cache and offloaded
`0/41` model layers to the GPU:

```text
granite4.1:8b
40 GB
65%/35% CPU/GPU
CONTEXT 131072
```

The default profile executed the real Claude Code tool loop but is excluded
from the full-GPU shortlist.

The local profile bounds the context for this RX 6800 stand:

```text
FROM granite4.1:8b

PARAMETER num_ctx 32768
```

## Tuned-Profile Result

| Stage | Seconds | Prompt marker observed | Real tool loop | Placement | Verdict |
|---|---:|---|---|---|---|
| `handshake` | `46.729` | Yes | n/a | `100% GPU` | Pass |
| `tool` | `84.495` | Yes | Yes | `100% GPU` | Pass |
| `agent` | `217.067` | Yes | Yes | `100% GPU` | Pass |

Placement evidence:

```text
granite4.1:8b-ctx32k
14 GB
100% GPU
CONTEXT 32768
```

Ollama server log:

```text
offloaded 41/41 layers to GPU
ROCm0 model buffer size = 4876.27 MiB
ROCm0 KV buffer size = 5120.00 MiB
```

The agent stage used structured tools, read the repository, created the proof
file, and reported the observed git status marker.

## Decision

Add `granite4.1:8b-ctx32k` as the third Phase 2 candidate for
`win11-local-rx6800`.

Keep an explicit semantic and encoding review in Phase 2: the generated proof
file contained a mojibake rendering of a non-ASCII hyphen.
