# Run Record: `qwen2.5-coder:14b` Full-GPU Gate

## Identity

- Date: `2026-06-01`
- Stand: `win11-local-rx6800`
- Endpoint: `http://localhost:11434`
- Model: `qwen2.5-coder:14b`
- Model details: `14.8B`, `Q4_K_M`
- Capabilities: completion, tools, insert
- Artifacts: `.artifacts/claude-ollama-headless/20260601T204423/`
- Runner option: `-RequireFullGpu`

## Result

| Stage | Seconds | Prompt marker observed | Real tool loop | Placement | Verdict |
|---|---:|---|---|---|---|
| `handshake` | `88.029` | Yes | n/a | `18%/82% CPU/GPU` | Fail: CPU spill |
| `tool` | `90.812` | Yes | No | `18%/82% CPU/GPU` | Fail: printed tool-call JSON and CPU spill |
| `agent` | `98.282` | No | No | `18%/82% CPU/GPU` | Fail: printed `Read` JSON, no workspace action, and CPU spill |

## Placement Evidence

```text
qwen2.5-coder:14b
18 GB
18%/82% CPU/GPU
CONTEXT 32768
```

Ollama server log:

```text
offloaded 39/49 layers to GPU
CPU KV buffer size = 1152.00 MiB
ROCm0 KV buffer size = 4992.00 MiB
```

## Tool-Loop Evidence

The model printed a Bash request as plain text:

```json
{
  "name": "Bash",
  "arguments": {
    "command": "python -c \"print('HEADLESS_TOOL_OK')\""
  }
}
```

It did not emit a Claude Code structured `tool_use` block.

The agent stage failed similarly by printing a `Read` request as JSON text.
No target file was created.

## Decision

Exclude `qwen2.5-coder:14b` from the next coder fixture suite on
`win11-local-rx6800`.

It fails both current gates:

1. no CPU or RAM spill
2. real Claude Code tool execution

Replacement candidate:

```text
qwen2.5-coder:7b
```

The official Ollama artifact is approximately `4.7 GB` and advertises tools
support. It still requires a local full-GPU and structured-tool verification
run.
